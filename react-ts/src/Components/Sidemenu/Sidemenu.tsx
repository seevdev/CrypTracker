import React from 'react';
import classes from './Sidemenu.module.scss';

const Sidemenu: React.FC = (props) => {
  return <menu className={classes.menu}>Menu Sidebar</menu>;
};

export default Sidemenu;
